import SignUp from "@/components/sign-up"; // Import the SignUp component

const SignUpPage = () => {
  return (
    <div className="relative">
      <SignUp /> {/* Render the SignUp component */}
    </div>
  );
};

export default SignUpPage; // Default export for the page
